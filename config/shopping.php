<?php

return [

    /*
    |--------------------------------------------------------------------------
    | HttpOnly cookie: opaque public_id сессии покупок (гость / до merge).
    |--------------------------------------------------------------------------
    */
    'session_cookie' => env('SHOPPING_SESSION_COOKIE', 'gangsters_shopping_session'),

    /*
    |--------------------------------------------------------------------------
    | Срок жизни сессии (дней). Продлевается при активности (middleware).
    |--------------------------------------------------------------------------
    */
    'session_ttl_days' => (int) env('SHOPPING_SESSION_TTL_DAYS', 90),

];
