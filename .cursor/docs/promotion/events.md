# Promotion — события

Доменных событий нет. Конфигурация меняется редко из Filament; подписчиков в других BC на старте не требуется.

При появлении кэша публичных порогов можно слушать `updated` модели `PRM_configuration` на уровне Infrastructure — вне домена Promotion.
