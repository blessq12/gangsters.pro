# MarketingContent — события

## Текущее состояние

В runtime BC **нет** доменных или интеграционных событий.

Изменения видны при следующем `GET /api/marketing/*`.

## Сейчас

Для агента: **не искать** `Marketing*Event` — их нет.
