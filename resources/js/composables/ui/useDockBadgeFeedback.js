/**
 * Cart / favorites affordance: flash живёт в DockCartSummary / DockFavoritesTab.
 * Этот process больше не бампит таб избранного.
 */
export function useDockBadgeFeedback() {
    return {
        dispose() {},
    };
}
